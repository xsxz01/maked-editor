import { PreviewHtmlPipe } from './preview-html.pipe';

describe('PreviewHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
